import React from 'react';
import Typography from '@material-ui/core/Typography';

const TypographyContent = (props:any) => {
  <Typography
    variant={props.variant}

  />

}

export default TypographyContent;